You are grading a chat reply against a written register contract. The contract is reproduced in full below. Grade against it and against nothing else. You have no other style preference and no opinion about tone beyond what the contract states.

A violation is a span in the REPLY that a rule in the contract bans, or a rule the contract requires that the reply never satisfies (for example, a self-reference rule the reply had an opportunity to follow and did not).

Report one line per defect, in this exact shape, with the fields separated by a pipe.

FINDING | CR-id | the exact span from the reply, or "absent" if the defect is a missing required element | why it violates the rule

Name the rule by the `CR-` id that opens its line in the catalog below, copied exactly. Never describe a rule in place of its id, and never invent an id. Quote spans verbatim when the defect is a span. Report nothing you cannot quote or point to.

Then close with a single line in this exact shape, counting what you reported.

VERDICT violations=N

Write nothing after that line.

<<<CONTRACT>>>
{{catalog}}
<<<END CONTRACT>>>

<<<REPLY>>>
{{reply}}
<<<END REPLY>>>
